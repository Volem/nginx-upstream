{
  "name": "nginx-upstream",
  "version": "0.1.4",
  "description": "Nginx Config File Manager",
  "main": "index.js",
  "dependencies": {
    "bluebird": "^3.5.0",
    "bunyan": "^1.8.10",
    "debug": "^2.6.3",
    "nginx-conf": "^1.3.0",
    "secure-callback": "^1.1.12",
    "typescript": "^2.2.2"
  },
  "devDependencies": {
    "eslint": "^3.19.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0"
  },
  "scripts": {
    "test": "rm -rf log && mkdir log && set DEBUG=development && mocha --colors -u tdd",
    "coverage": "istanbul cover node_modules/mocha/bin/_mocha --color test/test.js",
    "style": "eslint index.js test/*.js",
    "stylefix": "eslint --fix index.js test/*.js"
  },
  "keywords": [
    "nginx",
    "manage",
    "update",
    "config",
    "file"
  ],
  "author": "volem",
  "license": "MIT",
  "repository": {
    "url": "https://github.com/Volem/nginx-upstream",
    "type": "git"
  }
}
